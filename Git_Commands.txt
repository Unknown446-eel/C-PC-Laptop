git config --global	
				user.name
				user.email
				core.editor "code --wait"

git config --list --show-origin

git status 			//get status of files in working tree
git add .				//adds all files in directory to staging area 
git commit -m "Comment" 	//adds files in staging area to git 
git pull				//pulls from the git hub
